<Window x:Class="MeetingTransfer.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MeetingTransfer.App"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Settings"
        Width="1180"
        Height="780"
        MinWidth="960"
        MinHeight="640"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0" IsItemsHost="True" Margin="0,0,0,18" />
                            <ContentPresenter Grid.Row="1" ContentSource="SelectedContent" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabChrome" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabChrome" Property="Background" Value="{StaticResource SurfaceHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabChrome" Property="Background" Value="{StaticResource SurfaceRaisedBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Badge" TargetType="Border">
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Background" Value="{StaticResource SurfaceRaisedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineStrongBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Settings" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Style="{StaticResource IconButton}" Content="—" Click="Minimize_Click" />
                    <Button Style="{StaticResource IconButton}" Content="□" Click="Maximize_Click" />
                    <Button Style="{StaticResource IconButton}" Content="×" Click="CloseWindow_Click" Margin="0,0,4,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,20,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="2,0,0,18">
                <TextBlock Text="Preferences" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="26" FontWeight="SemiBold" />
                <TextBlock x:Name="SettingsPathText" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <TabControl Grid.Row="1">
                <TabItem Header="Speech models">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="470" />
                            <ColumnDefinition Width="14" />
                            <ColumnDefinition Width="350" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="9">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid Margin="16,13">
                                    <TextBlock Text="MODEL LIBRARY" Style="{StaticResource EyebrowText}" />
                                    <TextBlock Text="Offline, realtime, and feature resources" Foreground="{StaticResource SubtleBrush}" FontSize="11" HorizontalAlignment="Right" />
                                </Grid>
                                <Separator Grid.Row="0" VerticalAlignment="Bottom" />
                                <ListBox x:Name="ModelsList"
                                         Grid.Row="1"
                                         ItemsSource="{Binding CardsView}"
                                         Padding="8"
                                         VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                                         SelectionChanged="ModelsList_SelectionChanged">
                                    <ListBox.GroupStyle>
                                        <GroupStyle>
                                            <GroupStyle.HeaderTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="10,16,10,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource AccentBrush}" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center" />
                                                        <Border Grid.Column="1" Height="1" Background="{StaticResource LineBrush}" Margin="12,0,0,0" VerticalAlignment="Center" />
                                                    </Grid>
                                                </DataTemplate>
                                            </GroupStyle.HeaderTemplate>
                                        </GroupStyle>
                                    </ListBox.GroupStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="10,9">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Margin="0,0,8,0" />
                                                    <WrapPanel Margin="0,6,0,0">
                                                        <Border Style="{StaticResource Badge}">
                                                            <TextBlock Text="{Binding Family}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                                                        </Border>
                                                        <Border Style="{StaticResource Badge}">
                                                            <TextBlock Text="{Binding EngineLabel}" Foreground="{StaticResource AccentBrush}" FontSize="10" FontWeight="SemiBold" />
                                                        </Border>
                                                        <Border Style="{StaticResource Badge}" Visibility="{Binding IsGpuBackend, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" Background="#152B20" BorderBrush="#28513B">
                                                            <TextBlock Text="GPU" Foreground="{StaticResource SuccessBrush}" FontSize="10" FontWeight="SemiBold" />
                                                        </Border>
                                                        <Border Style="{StaticResource Badge}" Visibility="{Binding IsCpuBackend, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" Background="#2A2118" BorderBrush="#4D3926">
                                                            <TextBlock Text="CPU" Foreground="{StaticResource WarningBrush}" FontSize="10" FontWeight="SemiBold" />
                                                        </Border>
                                                    </WrapPanel>
                                                    <TextBlock Margin="0,4,0,0" Foreground="{StaticResource MutedBrush}" FontSize="11">
                                                        <Run Text="{Binding SizeDisplay, Mode=OneWay}" /><Run Text="  ·  " /><Run Text="{Binding LanguagesDisplay, Mode=OneWay}" /><Run Text="  ·  " /><Run Text="{Binding ExecutionMode, Mode=OneWay}" />
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap" Visibility="{Binding StatusMessage, Converter={x:Static local:NullToVisibilityConverter.Instance}}" />
                                                    <ProgressBar Minimum="0" Maximum="1" Value="{Binding DownloadProgress}" Margin="0,7,0,0" Visibility="{Binding IsDownloading, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="14,0,0,0">
                                                    <Button Content="{Binding PrimaryActionLabel}" Command="{Binding DataContext.PrimaryActionCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" MinWidth="132" IsEnabled="{Binding CanPrimary}" />
                                                    <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Style="{StaticResource QuietButton}" MinWidth="132" Margin="0,3,0,0" Visibility="{Binding CanDelete, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" />
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="9" Padding="19">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="MODEL DETAILS" Style="{StaticResource EyebrowText}" />
                                    <TextBlock Text="{Binding SelectedCard.DisplayName}" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="21" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,9,0,0" />
                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Border Style="{StaticResource Badge}"><TextBlock Text="{Binding SelectedCard.Family}" Foreground="{StaticResource MutedBrush}" FontSize="10" /></Border>
                                        <Border Style="{StaticResource Badge}"><TextBlock Text="{Binding SelectedCard.EngineLabel}" Foreground="{StaticResource AccentBrush}" FontSize="10" FontWeight="SemiBold" /></Border>
                                        <Border Style="{StaticResource Badge}" Visibility="{Binding SelectedCard.IsGpuBackend, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" Background="#152B20" BorderBrush="#28513B"><TextBlock Text="GPU" Foreground="{StaticResource SuccessBrush}" FontSize="10" /></Border>
                                        <Border Style="{StaticResource Badge}" Visibility="{Binding SelectedCard.IsCpuBackend, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" Background="#2A2118" BorderBrush="#4D3926"><TextBlock Text="CPU" Foreground="{StaticResource WarningBrush}" FontSize="10" /></Border>
                                    </StackPanel>
                                    <Grid Margin="0,18,0,0">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="SIZE" Style="{StaticResource EyebrowText}" /><TextBlock Text="{Binding SelectedCard.SizeDisplay}" Margin="0,5,0,0" /></StackPanel>
                                        <StackPanel Grid.Column="1"><TextBlock Text="MODE" Style="{StaticResource EyebrowText}" /><TextBlock Text="{Binding SelectedCard.ExecutionMode}" Margin="0,5,0,0" /></StackPanel>
                                    </Grid>
                                    <Separator Margin="0,18,0,16" />
                                    <TextBlock Text="DESCRIPTION" Style="{StaticResource EyebrowText}" />
                                    <TextBlock Text="{Binding SelectedCard.Description}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" LineHeight="19" Margin="0,7,0,0" />
                                    <TextBlock Text="SPEED" Style="{StaticResource EyebrowText}" Margin="0,18,0,0" />
                                    <TextBlock Text="{Binding SelectedCard.SpeedNote}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" LineHeight="19" Margin="0,7,0,0" />
                                    <TextBlock Text="ACCURACY" Style="{StaticResource EyebrowText}" Margin="0,18,0,0" />
                                    <TextBlock Text="{Binding SelectedCard.AccuracyNote}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" LineHeight="19" Margin="0,7,0,0" />
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="Tools">
                    <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="9" Padding="20" MaxWidth="840" HorizontalAlignment="Left">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="520" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="ffmpeg executable" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                            <TextBox x:Name="FfmpegPathBox" Grid.Row="0" Grid.Column="1" Margin="0,0,8,8" />
                            <Button Grid.Row="0" Grid.Column="2" Content="Browse" Tag="FfmpegPathBox" Click="BrowseFile_Click" Margin="0,0,0,8" />
                            <TextBlock Grid.Row="1" Text="Online executable" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                            <TextBox x:Name="OnlineExeBox" Grid.Row="1" Grid.Column="1" Margin="0,0,8,8" />
                            <Button Grid.Row="1" Grid.Column="2" Content="Browse" Tag="OnlineExeBox" Click="BrowseFile_Click" Margin="0,0,0,8" />
                            <TextBlock Grid.Row="2" Text="Online arguments" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                            <TextBox x:Name="OnlineArgsBox" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />
                            <TextBlock Grid.Row="3" Text="Offline language" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                            <ComboBox x:Name="OfflineLanguageBox" Grid.Row="3" Grid.Column="1" SelectedValuePath="Tag">
                                <ComboBoxItem Content="Bilingual zh/en" Tag="bilingual" />
                                <ComboBoxItem Content="Chinese zh" Tag="zh" />
                                <ComboBoxItem Content="English en" Tag="en" />
                            </ComboBox>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Updates">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="560" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="9" Padding="24" VerticalAlignment="Top">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="APPLICATION UPDATES" Style="{StaticResource EyebrowText}" />
                                        <TextBlock Text="EchoMinutes" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="24" FontWeight="SemiBold" Margin="0,8,0,0" />
                                        <TextBlock Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0">
                                            <Run Text="Installed version  " /><Run x:Name="CurrentVersionText" FontWeight="SemiBold" Foreground="{StaticResource InkBrush}" />
                                        </TextBlock>
                                    </StackPanel>
                                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="48" Height="48" CornerRadius="24" Background="{StaticResource AccentSurfaceBrush}" BorderBrush="#284A78" BorderThickness="1">
                                        <TextBlock Text="↻" FontSize="24" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </Grid>
                                <Separator Grid.Row="1" Margin="0,22,0,18" />
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                                        <TextBlock x:Name="UpdateStatusText" Text="Updates are checked automatically after startup." TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" />
                                        <TextBlock Text="Source: github.com/luckykevvv/echo-minutes/releases" Foreground="{StaticResource SubtleBrush}" FontSize="11" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <Button x:Name="CheckForUpdatesButton" Grid.Column="1" Content="Check for updates" Width="154" Style="{StaticResource AccentButton}" Click="CheckForUpdates_Click" />
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>

            <DockPanel Grid.Row="2" Margin="0,18,0,0">
                <TextBlock Text="Changes are saved beside the application." Foreground="{StaticResource SubtleBrush}" FontSize="11" VerticalAlignment="Center" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Cancel" Width="92" Click="Cancel_Click" Style="{StaticResource QuietButton}" Margin="0,0,8,0" />
                    <Button Content="Save changes" Width="118" Click="Save_Click" Style="{StaticResource AccentButton}" />
                </StackPanel>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
